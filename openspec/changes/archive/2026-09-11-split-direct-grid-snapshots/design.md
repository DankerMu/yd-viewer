## Context / baseline
Issue114 explicitly requires compatibility facades (this is not the default clean-cutover migration). Parent baseline before edit: producer.py4067, file_store.py1852, test entry1764lines; package__all__/module names/class+method signatures in .workplans/issue-114/baseline-api.json; exact17node list baseline-nodeids.json; every callable AST/classstate in baseline-ast.json; originalguard in baseline-exemptions.json. Reconnaissance maps source dependencies and 53 historical mutation legs. No super()/__class__/type(self)/explicitclass-self references in movedmethods; no dataclass postinit; source helper graph can be kept acyclic.

## Risk triage
Expanded fixture with broad-expanded repair intensity/effective tier: multiple shared helper families across mapping/canonical read/output/reuse and repository registry/catalog/readiness/handoff. Four seats correctness,invariant-state,test-evidence+spec-compliance,security-perf+integration. Legacy issue has no suggestedfixture/minimal-slice metadata; its declared three-file pure split plus guard removal is consumed as one slice.
Selected: PublicAPI/facade, config setup(existing constructor/defaults and lint/guard routing), schema/columns/units(bytes/dataclasses unchanged), legacycompat(imports/17IDs), error/partialoutput(no-ready), fileIO/path/security(existing guards), resourcebound(existingcaps and strictmodulelimit), documentation/provenance, geospatial/time-series/NWM-DBfree. Auth/secrets/UI/release/dependencies/distributedcontroller notselected: no corresponding algorithm/interface/dependency changes. Publication/rollback/state semantics are preserved, not redesigned.

## Placement / import DAG
placement.json is the exact symbol/method/global source-of-truth; paths are relative to forcing/ or tests/ by section. Every original definition has exactly one owner; facades only re-export it, never duplicate implementation.
Producer targets: producer.py concrete ForcingProducer keeps __init__,produce and all four canonical grid/field/dataset-grid methods; nine leaf modules _producer_types, _producer_common, _producer_stations, _producer_grid, _producer_contracts, _producer_inputs, _producer_timeseries, _producer_output, _producer_reuse. Five disjoint stateless method carrier classes are _ContractMethods/_InputMethods/_TimeseriesMethods/_OutputMethods/_ReuseMethods, inherited by the original concrete class. They add no fields/init/hooks/super/delegation wrappers; moved method ASTs/signatures/decorators remain identical. Data/exception/protocol/config class bodies move intact into types. Lower DAG: types -> common -> stations/grid/timeseries -> reuse -> output/contracts/inputs -> concretefacade (arrows describe dependency availability, not all direct edges). common owns _json_round_trip/_stable_identity/_products to avoid reuse/common/time cycles.
File-store targets: original dataclass FileForcingRepository, full field block and all MAX_OBJECT_MANIFEST_BYTES read sites remain file_store.py. Move only _canonical_product_from_path/_validate_canonical_catalog_envelope/_canonical_product_from_catalog_row to _file_store_catalog._CatalogMethods; four handoff methods to _file_store_handoff._HandoffMethods; pure helpers/constants/error to _file_store_common. MAX and LOGGER stay facade. Dataclass init/eq/repr/cache defaults unchanged; mixins have no state. file_store -> producer direction remains, never producer -> file_store.
Tests: all13 test_* definitions/decorators stay original file. forcing_seed_repository owns FakeForcingRepository/four fake helpers/DIRECT_GRID_CACHE_STATION_ROLE; forcing_seed_assets owns pure file/canonical/direct-grid asset builders; forcing_seed_builders owns composition/assertion helpers. Assets and fake do not import entry/builders; builders depend on them; entry imports/re-exports baseline support names. No supportfilename starts test_, no duplicatecollection.

## Compatibility / allowed structural edits
Keep every baseline nondunder module attribute importable, package__all__ exact and existing constructors/method signatures unchanged. Explicit imports/re-exports, class bases, module headers, import wiring and stateless carrier docstrings are allowed. Traceback/source-location and moved definition __module__/__qualname__ metadata naturally relocate; no persisted pickle protocol is introduced. Concrete public ForcingProducer/FileForcingRepository stay original modules. No dynamic exec/FunctionType/global proxy/lazy __getattr__ shim/runtime wrapper.
Protected sites stay original modules: producer.LocalObjectStore + __init__; producer.open_canonical_netcdf + _read_canonical_grid/_read_canonical_field; file_store.MAX_OBJECT_MANIFEST_BYTES + every directreader (_final_readiness_evidence_is_coherent/_stations_from_model_manifest/_canonical_products_from_catalog/_read_json_reference). Existing monkeypatches and leg36 sentinel must still hit live read sites, without companiontest edits. LOGGER in facades unchanged; leaf LOGGER, only where needed, uses getLogger(original facade module string), same cached logger identity without importcycle.
Every original function/method AST, dataclass/protocol body, field/default/constant/regex/format string, decorator and seed assertion must remain identical. Preserve original comments and existing registered yd adaptations. Two _json_bytes remain distinct: canonical_json ASCII escaping vs file-store nativeUTF8; never consolidate. Keep #104lowercaseURI, #119reprgeometry vs .10gseries, #127Darwinmemory exactly.

## Provenance and lint boundaries
Every new extracted module begins exact whole-line # NWM@8ae9b8f2 plus its original producer.py/file_store.py/test_forcing_producer.py source path within first5lines; inventory gets one unique target row, source may repeat. New carrier shells/imports are explicitly labelled yd structural glue; moved snapshot/previouslyregistered adaptation bodies are not presented as newlyported code. No new yd scientific/validation helper or falsepin marker. Originalfacadeheaders stay; file_store stale inventoryrow number becomes targetpath identity. Inventory pendingrows publish beforecode, then flip tolanded with actualfiles beforematrix; tablecount/reversecapabilitynotes follow actualtargetset. Provenanceguard remains strict and unchanged unless a concrete parser incompatibility is discovered (not expected).
Existing exactpath Ruff ignored-rule sets are pin snapshot lint baselines, NOT large-file exemptions. May carry only a parent file's existing rules to exact registered descendantpaths where unchanged movedcode needs them; no new ruleids/globs/yd-code exemptions. Prefer accurate imports over gratuitous ignoredunusednames; oldfacades preserve publicimports. Do not fix old executable style under the guise of lint. large-file config changes only removal of the three requested entries; maxLines1000 and other11excludes remain exactly.

## Invariant Matrix
Governing invariant: structural relocation preserves one canonical definition and every observable forcing/import/oracle contract while all18 old/new snapshotfiles are <1000 and admitted to the unchanged size guard.
Source identity: baseline ASTs + placement owner + exact NWM source header/inventory row + package/module import names.
- Producers: all43 ForcingProducer methods and shared models/helpers preserved once; existing constant/default/units/time/grid/lineage outputs unchanged.
- Validators: same request/source/grid/JSON/cap/path guards in same call order; no new exceptions or bypass.
- Store/cache/query: FileForcingRepository field/default/cache state and methods preserved; catalog/handoff helpers only move.
- Public seams: package__all__, oldconcrete modules, constructor/method signatures, protected mutable/injection globals preserved.
- Consumers: allforcing companions/assemble/converter imports continue unchanged; all13seeds and fake/helperclosures preserved.
- Failure/reuse/publication: original failure/no-ready/ready/reuse/cleanup transitions and ordering unchanged; mutation53legs still discriminate.
- Evidence: exact17node list, perdefinitionAST one-to-one, import/DAG/scope audit, bothserializers' literalSHA, current53mutationkills, fullmatrix, actualstagedguard.
Separate regression obligations:
1. Import oldpackage/producer/file_store -> allbaseline symbols and signatures available, classaliases singleidentity, protectedpatch tests hit actualreaders.
2. Collect original test entry -> exactordered17baseline IDs,13testdefinition ASTs identical, no duplicate collection in fullsuite.
3. Reassemble every original callable by placement -> sameAST exactlyonce; unchanged dataclassstate/constants, no missing/duplicate body.
4. Every original/new snapshotfile -> strict<1000 plus exactheader/inventorybidirectional; threeexclusions removed and allothers/threshold unchanged.
5. All53 historical numbered mutationlegs -> actual red test(s) recorded on split source; calibrateweightfirst, restorebytes eachleg and finalbaselinegreen. No prewritten successcounts.
6. Existing full producer/viewer/ruff/OpenSpec/stage matrix -> green, no independentliteral/failure oracle weakened.

## Mutation proof contract
Consume complete .workplans/14/mutation-matrix.md (18phase1 +14repair +3depth +1aliaswiring +1dynamicsentinel +15boundary +1realCanonicalProductfield =53), not just headline8mutants. Additional unnumbered historical sibling notes are not part of that53table, and their existing tests remain unchanged. Parent makes a per-leg manifest mapping oldanchor -> placement/currentanchor -> actualnodeid/result/restoreproof. The old uppercaseIFS URI legs are superseded by merged#104: use current lowercasenodeids; mutate consumedURI to uppercase (not no-oplowercase) and retain independent12Zcycle mutation. Descriptorfallback leg exercises existing forcedLinux aliasfailure test after#127; do not remove Darwinsuccesscontract. JSONbyte/depth/node and bothserializer directions remain separate. Configlazy/eager and bothlineage keys remain separate. Retain legs36/37 fixture-sentinel discrimination, no assertion deletion.
Each mutant runs fresh uv Python in an isolated source/docs/openspec copy without .venv/pyc/cache; assert package AND targetdefinition resolve inside scratch and mutation anchor is active. uv run python -m pytest or pytest.main, never console-scriptpytest. Disablebytecode, exactbyte restore aftereach; no integration-source/index/stash mutation. Run baseline killer nodes, weightcalibration, all53 independently, final restoredgreen. Persist exactscripts/logs/manifest; remove throwaway sources afterproof. Existing tests should supply killers; if a real gap appears, report/fix via workflow, not weakenproof.

## Boundary checklist / verification
Shared roots: new leafmodules only; old concretefacades own injectionglobals. Publicimports/read/write/reuse paths unchanged. No newstaging/publish/delete/rollback/locks/nativeIO handoff. Downstreamcanonical/assemble/state/controller behavior outside this structuralchange. Parent runs import/AST/17collection checks, existingfocusedforcing/provenance suites, 53mutants, direct realfixture production smoke, then serialfullmatrix. Finally stage all18old/newtargets+guardconfig and invoke actual large-file guard with git-commit tool JSON; independentlyassertstrict<1000 because hook itself rejects only>1000.

## Rollback / non-goals
Revert splitPR restores oldlayout/exclusions; no product/schema migration. No scientific fixes, newtests for newbehavior, M4/liveNWM/node operations, dependency changes, orphaned deprecated paths or cleanup of other exemptions. Compatibilityfacades are explicitly required usercontract, not temporary shims todelete later.

Exact inherited URI killer migration: tests/test_forcing_round1_closure_yd.py::test_ifs_fixture_uses_literal_12z_and_exact_lowercase_grid_uri and ::test_ifs_grid_read_receives_exact_lowercase_catalog_uri replace the old uppercase-name references. The URI mutation changes consumed lowercase to uppercase; cycleleg still changes12Z parsing to00Z. No coverage deletion.
Pre-split actualproduction smoke already ran via _prepared_file_seam (real FileForcingRepository+ForcingProducer): forcing_ready then already_done, byte-identical complete forcing/runs artifacts across reuse. Baseline hashes and exact standalone runner are .workplans/issue-114/baseline-production-smoke.json and production-smoke-runner.txt; post-split run must exactly match these outputhashes/statuses.

## Phase2 oracle-discriminator repair (no runtime change)
Actual53-leg execution found leg4 (remove catalog row/request source comparison) survives: the old row-source case changes only source_id, leaving gfs productID/objectURI, so later identity checks reject it for a different reason. Strengthen ONLY that existing companion parametrized row-source input in test_forcing_contract_boundaries_yd.py: seed real coherent IFS canonical products via existing canonical_products_for_cycle at00Z/ifs_0p25, take an actual IFS catalog row and place it in the GFS envelope. Keep the same row-source/row-cycle IDs and _assert_catalog_refused (repository refusal + public no-ready) unchanged; row-cycle branch unchanged. Real foreign bytes/attrs/checksum/id/key are coherent; request-source mismatch is the isolated defect. No new test function, no primary13seed/body or runtime source change. This narrowly scoped existing-input strengthening is required to restore inheritedmutation discrimination, not a forcing semantic change. All other callableAST obligations remain.
Leg27 initially targeted the producer fallback text decoder, but the file repository is the earlier live sp.att decoder for its oracle; corrected manifest targets FileForcingRepository.load_direct_grid_validation_assets. No source/test change for this anchor correction. Harness setup/error-log classification corrections are recorded separately; no surviving leg is accepted as a kill.
