Change surface: `viewer/src/yd_viewer/basemap.py` (new route + cache + cooldown), `settings.py` (two optional envs), `app.py` (conditional registration before the `/api` 404 mount), `viewer/entrypoint.sh` (proxy mode), `viewer/frontend/src/lib/basemaps.ts` (resolve tile URLs), `viewer/compose.example.yml`, `viewer/env.example`, `viewer/Dockerfile` (`/cache`).
Must preserve: without `YD_TIANDITU_KEY` everything is byte-for-byte the old behavior — six URL envs → `basemaps.json`, direct tiles, `/api/basemap/...` 404 (not `index.html`); the four existing API endpoints, `/geometry/*` and `/` unchanged; `YD_VIEWER_STATIC_DIR` still not in the ops env list; frontend absolute tile URLs unchanged; no new runtime dependency (dev group keeps `httpx2`).
Must add/change: `GET /api/basemap/tianditu/{layer}/{z}/{x}/{y}`: `layer ∈ {vec,cva,img,cia,ter,cta}`, `0≤z≤18`, `0≤x,y<2^z`, else 404/422 (FastAPI default `{"detail":...}`); upstream `https://t{(x+y)%8}.tianditu.gov.cn/DataServer?T={layer}_w&x={x}&y={y}&l={z}&tk={key}` via `urllib.request` in `run_in_threadpool`, timeout 10 s, fixed browser `User-Agent`, no `Referer`/`Cookie`; cache file `<cache_dir>/<layer>/<z>/<x>/<y>` (no extension; content type sniffed from the PNG/JPEG signature on read and write); hit → 200 body + `Cache-Control: public, max-age=604800` + `X-Tile-Cache: hit`; miss → fetch, if 200 and body starts with PNG/JPEG signature → write `tmp-<uuid>` in the same dir then `os.replace`, respond `X-Tile-Cache: miss`; upstream 429 → set `_throttled_until[layer] = monotonic()+60`, respond 503 `no-store`; other non-200 / non-image / `URLError` / timeout → 502 `no-store` (429 → 503), nothing written; while a layer is cooling → 503 `no-store` without an upstream call (cache hits are still served, checked before the cooldown). Key appears only in the upstream URL; log lines and error bodies never include it. Entrypoint proxy mode: `YD_TIANDITU_KEY` non-empty ⇒ JSON `{"vector":{"tiles":["api/basemap/tianditu/vec/{z}/{x}/{y}"],"annotation":["api/basemap/tianditu/cva/{z}/{x}/{y}"]},"satellite":{img,cia},"terrain":{ter,cta}}`. Frontend: `entry.tiles`/`entry.annotation` mapped through a template-safe `resolveTileUrl(pageDir, url)`: absolute URLs (`/^[a-z][a-z0-9+.-]*:/i`) unchanged; otherwise plain string concatenation `pageDir + url` where `pageDir` is the page URL up to and including its last `/` — NOT `new URL()`/`resolveUrl`, which percent-encodes `{z}/{x}/{y}` into `%7Bz%7D…` and MapLibre then never substitutes them (fixture review 2026-09-22).
Governing invariant: a tile body is served from disk only if it was fetched as a 200 image; anything else is neither cached nor cacheable by the browser.
Sibling surfaces: the `/api` 404 mount in `app.py` (route must be registered before it); `StaticFiles` mounts (unchanged); `test_entrypoint.sh` (both modes); `env.example`/compose assertions: none exist in `viewer/tests` today, so a new `viewer/tests/test_container_contract.py` is added; frontend `basemaps.test.ts`; `viewer/tests/test_container_contract.py` (NEW — no compose/env test exists today); node-27 compose (`/home/nwm/yd-viewer/compose.yml`, separate site step).
Seams under test: FastAPI TestClient against `create_app(settings)` with `urllib.request.urlopen` monkeypatched (returns a fake response with `.status`, `.read()`, `.headers`); a `tmp_path` cache dir; `time.monotonic` monkeypatched for the cooldown; shell test of `entrypoint.sh` with/without the key; vitest for `basemaps.ts` resolution.
Required evidence: valid request miss → upstream called once with the expected URL/headers → 200 image, file exists, `X-Tile-Cache: miss`; second request → hit, upstream not called; upstream 500 → 502 `no-store`, no file; upstream 429 → 503 `no-store`, next request of the same layer within 60 s → 503 with zero upstream calls, other layer still fetches; upstream 200 with HTML body → 502, no file; bad layer/zoom/coords → 4xx; no key → `/api/basemap/tianditu/vec/1/0/0` 404 non-HTML; key never in response body or captured log; entrypoint proxy mode JSON exact; frontend relative → absolute.
Non-goals: cache pruning/quota; other tile providers; MVT; NWM-side changes; node-27 deployment.
Review focus: (1) cache path containment (validated ints + literal layer; no user string reaches the path); (2) never-cache-failures and `no-store` on every failure path; (3) per-layer cooldown isolation and monotonic clock; (4) key never logged (also on exceptions); (5) route precedence over the `/api` 404 mount and old behavior when the key is unset.
